export const ExplanationFlow = { getMode: () => { if (typeof window === 'undefined') return 'AUTO'; return sessionStorage.getItem('ExplanationMode') === 'MANUAL' ? 'MANUAL' : 'AUTO'; }, setManual: () => { if (typeof window !== 'undefined') sessionStorage.setItem('ExplanationMode', 'MANUAL'); }, setAuto: () => { if (typeof window !== 'undefined') sessionStorage.setItem('ExplanationMode', 'AUTO'); }, setLastTimestamp: () => { if (typeof window !== 'undefined') sessionStorage.setItem('ExpLastTS', Date.now().toString()); }, isFreshVisit: () => { if (typeof window === 'undefined') return true; const ts = sessionStorage.getItem('ExpLastTS'); if (!ts) return true; return (Date.now() - parseInt(ts, 10)) > 3000; } };
